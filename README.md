# decentralized-lottery-game
A fully functional on-chain lottery system. Players enter by sending a fixed amount of ETH. The manager (or an automated script) triggers the winner selection algorithm, which randomly picks a winner and automatically transfers the contract balance to them.
